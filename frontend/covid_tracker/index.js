const PORT = 3000;
const app = require('express')();

app.get('/', (req, res) => {

});

app.listen(PORT, () => {
    console.log(`Application listening on port ${PORT}`);
});

//hello form Quang's computer